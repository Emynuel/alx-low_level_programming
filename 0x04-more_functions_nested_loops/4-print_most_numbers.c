#include "main.h"

/**
 * print_most_numbers - a function that print numbers
 * Return: Always 0
 */

void print_most_numbers(void)
{
	int a;

	a = 0;

	while (a < 10)
	{
		if (a != 2 && a != 4)
		{
			_putchar(i + '0');
		}
		i++;
	}

	_putchar('\n');
}
