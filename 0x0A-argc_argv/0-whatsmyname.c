#include "main.h"
#include <stdio.h>

/**
 * main - prints prograam's name followed by new line
 * @argc: Number of command line arguments
 * @argv: Array containing command line argument
 *
 * Return: Always (0)
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", *agrv);
	return (0);
}

